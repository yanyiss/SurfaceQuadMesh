#include "dualLoop.h"
namespace QuadLayout
{
	void dualLoop::initGraph()
	{
		/*int nv = mesh->n_vertices();
		for (int i = 0; i < 2; ++i)
		{
			MatrixXd &g = graph[i];
			g.resize(nv, nv);

		}*/

	}
}
